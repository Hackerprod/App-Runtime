# Android Runtime prototype

This repository contains a deliberately bounded Android-to-Windows runtime slice. It reads a real APK and binary manifest, resolves the launcher Activity, interprets DEX lifecycle methods through `Resumed`, and exposes an observable API-binding boundary. The Windows host proves one real HWND binding; the core remains portable `net8.0`.

## Architecture

Binding registration is organized per-package under `ApiLayer\Bindings\` (e.g. `JavaLangBindings.cs` for `java.lang`, `JavaLangThreadBindings.cs` for `java.lang.Thread`, `JavaUtilConcurrentAtomicBindings.cs`), generalized from the existing `AndroidSystemServiceBindings` shape; older bindings still live in `AndroidApiBindings.cs` and migrate as they are touched, not retroactively.

- **AndroidRuntime.Core (`net8.0`)**: APK/AXML/DEX parsing, interpreter, lifecycle state machine, immutable API registry snapshots, tracing, cancellation, execution lane, and host ports.
- **AndroidRuntime.WindowsHost (`net8.0-windows`, WPF)**: real HWND/Dispatcher adapter and command-line host. WPF types never enter Core.
- **Per-session isolation**: each hosted launch owns one execution lane, registry snapshot, Activity/window peer map, cancellation source, and bounded trace buffer.
- **Close ownership**: closing the Activity window starts complete session shutdown. Callers can await `Termination`; repeated `DisposeAsync` calls join the same cleanup operation.
- **Thread model (GIL)**: guest threads are real CLR threads, serialized by a per-session Global Interpreter Lock (`AndroidGil`) that any thread must hold while executing guest bytecode (modeled on CPython's GIL). The original execution-lane thread is the stable **main guest thread**; `Thread.start()` spawns real background threads that acquire the GIL, run the guest `run()` body, and release it. Blocking guest operations (`Thread.sleep`, `join`, `monitor-enter`, and the class-initialization wait) release the GIL while blocked and reacquire after, so other threads make progress. The explicit, accepted tradeoff: **no parallel guest bytecode execution** — this project targets compatibility, not throughput. Window operations synchronously marshal to the WPF Dispatcher; UI/toast bindings still require the main guest thread. The window exists and is associated before `onCreate`, is shown after `onStart`, and is visible before `onResume`.

### Exact API portfolio

| Owner | Exact methods/descriptors |
| --- | --- |
| `Object` | `<init>()V` (no-op super constructor for plain DEX classes) |
| `AtomicReference` | `<init>()V`; `<init>(Object)V`; `get()Object`; `set(Object)V`; `compareAndSet(Object,Object)Z` (plain reference-identity CAS, no real synchronization — single serial lane) |
| `WeakHashMap` | `<init>()V`; `put(Object,Object)Object`; `get(Object)Object`; `containsKey(Object)Z`; `remove(Object)Object`; `size()I` (bounded simplification: keys strongly referenced, no guest GC model; key equality is CLR equality, not guest-overridden equals()/hashCode()) |
| `HashMap` | `<init>()V`; `put(Object,Object)Object`; `get(Object)Object`; `containsKey(Object)Z`; `remove(Object)Object`; `size()I` (null key supported via internal sentinel; key equality is CLR equality, not guest-overridden equals()/hashCode()) |
| `ArrayList` | `<init>()V`; `<init>(I)V` (capacity accepted and ignored); `add(Object)Z`; `add(int,Object)V`; `get(I)Object`; `set(int,Object)Object`; `size()I`; `isEmpty()Z`; `remove(I)Object`; `contains(Object)Z`; `indexOf(Object)I`; `clear()V`; `iterator()Iterator` (out-of-range index ops throw guest IndexOutOfBoundsException; contains/indexOf use CLR equality) |
| `CopyOnWriteArrayList` | `<init>()V`; `<init>(Collection)V` (copies modeled guest collections); `add(Object)Z`; `add(int,Object)V`; `get(I)Object`; `set(int,Object)Object`; `remove(I)Object`; `remove(Object)Z`; `contains(Object)Z`; `indexOf(Object)I`; `size()I`; `isEmpty()Z`; `clear()V`; `addIfAbsent(Object)Z`; `iterator()Iterator` |
| `Iterator` | `hasNext()Z`; `next()Object` (snapshot iteration — captures elements at iterator() time; exhausted next() throws guest NoSuchElementException) |
| `Enum` | `<init>(String,I)V`; `name()String`; `ordinal()I`; `toString()String` (defaults to name); `equals(Object)Z` (reference identity); `hashCode()I` (CLR identity hash); `compareTo(Enum)I` (ordinal compare) |
| `Thread` | `<init>()V`; `<init>(Runnable)V`; `start()V` (spawns a real CLR thread under the GIL; double start throws IllegalThreadStateException); `run()V`; `join()V`/`join(J)V`; `interrupt()V`; `isInterrupted()Z`; `isAlive()Z`; `setName(String)V`; `getName()String`; static `currentThread()Thread`; static `sleep(J)V` (all blocking ops release the GIL; interrupt wakes sleep/join with guest InterruptedException) |
| `AtomicInteger` | `<init>()V`; `<init>(I)V`; `get()I`; `set(I)V`; `getAndIncrement()I`; `incrementAndGet()I`; `getAndDecrement()I`; `decrementAndGet()I`; `getAndAdd(I)I`; `addAndGet(I)I`; `compareAndSet(II)Z` (plain read/modify/write, no real synchronization — single serial lane) |
| `WeakReference` | `<init>(Object)V`; `get()Object`; `clear()V` (referent held strongly — no guest GC model, safe over-approximation like WeakHashMap) |
| `CopyOnWriteArraySet` | `<init>()V`; `add(Object)Z` (real Set dedup semantics; scoped by inspecting the APK's full method-reference table — only these two are ever referenced) |
| `Activity` | `<init>()V`; lifecycle stubs; `setTitle(CharSequence)V`; `getTitle()CharSequence`; `getLocalClassName()String`; `getIntent()Intent` |
| `Context` | `getPackageName()String`; `getApplicationContext()Context` |
| `Log` | `v/d/i/w/e/wtf(String,String)I`; `println(int,String,String)I`; `isLoggable(String,int)Z` |
| `TextUtils` | `isEmpty(CharSequence)Z`; `equals(CharSequence,CharSequence)Z`; `getTrimmedLength(CharSequence)I` |
| `String` | `length()I`; `isEmpty()Z`; `equals(Object)Z`; `equalsIgnoreCase(String)Z`; `startsWith(String)Z`; `endsWith(String)Z`; `contains(CharSequence)Z`; `indexOf(String)I`; `indexOf(String,int)I`; `concat(String)String`; `trim()String`; `toString()String`; `hashCode()I`; static `valueOf(I/Z/C)String` |
| `StringBuilder` | constructors `()`, `(I)`, `(String)`; `append(String/CharSequence/int/boolean/char)`; `length()I`; `toString()String` |
| `CharSequence` | `toString()String` |
| `Color` | `rgb(III)I`; `argb(IIII)I`; `alpha/red/green/blue(I)I` |
| `Bundle` / `BaseBundle` | constructors `()`, `(I)`, shallow copy; typed String/int/long/boolean put/get plus defaults; `containsKey`, `remove`, `clear`, `size`, `isEmpty` |
| `SystemClock` | `uptimeMillis()J`; `elapsedRealtime()J`; `elapsedRealtimeNanos()J` through an injected monotonic clock port |
| `Intent` | constructors `()`, `(String)`; action get/set; String/int/boolean extras; `hasExtra`; `removeExtra` |
| `Toast` | `makeText(Context,CharSequence,int)`; `show`; `cancel`; duration get/set; `setText(CharSequence)` |
| `Activity` UI | `setContentView(int)`; `findViewById(int)` |
| `View` | `findViewById(int)`; `getId`; enabled and visibility state; `setOnClickListener`; `performClick` |
| `TextView` / `Button` | `setText(CharSequence)`; `getText`; retained `LinearLayout`/`TextView`/`Button` peers |
| `Throwable` hierarchy | constructors `()`, `(String)`, `(String,Throwable)`; `getMessage()String`; `toString()String` for the bounded Error/Exception hierarchy |
| `Context` services | `getSystemService(String)Object`; exact stable per-session ClipboardManager and ConnectivityManager; unknown/adapter-absent returns null |
| `ClipData` / `Item` | `newPlainText(CharSequence,CharSequence)`; `getItemCount`; `getItemAt`; Item `getText`; `coerceToText(Context)` |
| `ClipboardManager` | `setPrimaryClip`; `getPrimaryClip`; `hasPrimaryClip`; `clearPrimaryClip` for one plain-text item |
| `ConnectivityManager` | `getActiveNetwork`; `getNetworkCapabilities`; honest `isActiveNetworkMetered` (unavailable when host cannot know) |
| `NetworkCapabilities` | `hasCapability(int)` for INTERNET/VALIDATED/NOT_METERED/NOT_RESTRICTED/NOT_VPN/NOT_ROAMING; `hasTransport(int)` for CELLULAR/WIFI/ETHERNET/VPN; unknown constants false |
| `Executors` | static `newFixedThreadPool(I)` / `newFixedThreadPool(I,ThreadFactory)` / `newSingleThreadExecutor()` / `newCachedThreadPool()` / `defaultThreadFactory()`. Pools are REAL thread pools: N real background workers (the same mechanism `Thread.start` uses) pulling guest Runnable/Callable tasks off a shared queue under the GIL. Cached pools grow up to a bounded max of 32 workers and idle workers exit after 60s (never truly unbounded — fail-closed). |
| `ExecutorService` / `Executor` | `execute(Runnable)V`; `submit(Callable)Future` / `submit(Runnable)Future`; `shutdown()V` (stop accepting, drain queued); `shutdownNow()List` (interrupts running workers best-effort, returns the not-started queue); `isShutdown()Z`; `isTerminated()Z`; `awaitTermination(J,TimeUnit)Z` (releases the GIL while waiting). Post-shutdown submissions throw guest `RejectedExecutionException`. |
| `Future` / `FutureTask` | `get()Object` / `get(J,TimeUnit)Object` (real blocking wait, releases the GIL; timeout throws guest `TimeoutException`); `isDone()Z`; `isCancelled()Z`; `cancel(Z)Z` (pending → never runs; running + mayInterrupt → cooperative CLR interrupt); cancelled `get()` throws guest `CancellationException`; a task that threw surfaces guest `ExecutionException`. |
| `TimeUnit` | JDK enum modeled as stable framework singleton constants (OUTSIDE the guest Enum machinery): all seven constants resolve via the interpreter's framework static-field hook (`sget`), plus `toMillis/toNanos/toSeconds(J)J`, `convert(J,TimeUnit)J`, `ordinal()I`, `toString()String`, static `values()` |
| `Looper` | static `getMainLooper()` (the stable main peer — under a hosted lane it reuses the lane's EXISTING message-loop queue, never a second pump); `myLooper()` (calling thread's Looper or null); `prepare()` / `loop()` (a background thread's own private queue pumped by that thread, releasing the GIL while waiting); instance `quit()` / `quitSafely()` |
| `Handler` | constructors `()`, `(Looper)` (no-arg throws guest `RuntimeException("Can't create handler inside thread that has not called Looper.prepare()")`); `post(Runnable)Z` (runs asynchronously on the bound Looper — never inline); `postDelayed(Runnable,J)Z` (cancellable timer); `removeCallbacks(Runnable)V`; `hasCallbacks(Runnable)Z`; `postAtFrontOfQueue(Runnable)Z` (enqueued like post; the shared lane queue has no priority reorder). No Message/sendMessage surface. |
| `Class` | canonical singleton per represented descriptor (const-class and `Object.getClass()` return the SAME object, so reference identity holds); `getName()String` (dotted binary name; real array/primitive contract — `[Lc.Foo;`, `int`); `getCanonicalName()String` (top-level/primitive/array; inner-class canonicalization not detected); `getSimpleName()String` (last segment, `Foo[]` for arrays); `isInstance(Object)Z` / `isAssignableFrom(Class)Z` (reuse the existing assignability machinery); `getSuperclass()Class` (reuses interpreter superclass resolution; null for Object/arrays/primitives); `getPackage()Package` (null for default package/primitives/arrays); `forName(String)Class` + `forName(String,boolean,ClassLoader)Class` (dotted binary name → descriptor, lookup across LOADED guest classes only, triggers `<clinit>` as the real documented side effect unless `initialize=false`, returns the canonical object; unknown OR framework-type names → checked `ClassNotFoundException` — framework types have no reverse name index, honest limitation); `equals/hashCode` (identity, free via the canonical cache); `toString()` ("class " + getName). `Enum.getDeclaringClass()` and `Enum.valueOf(Class,String)` closed now that Class exists (valueOf enumerates `<clinit>`-stored constants, throws guest IllegalArgumentException for a missing name). NO reflection/`newInstance` (see boundary #39). |
| `String` | migrated to `ApiLayer\Bindings\JavaLangStringBindings.cs`; `length`, `isEmpty`, `equals`, `equalsIgnoreCase` (Java-pinned Unicode mapping), `startsWith` (+offset overload), `endsWith`, `contains`, `indexOf` (String/String+from/char/char+from), `concat`, `trim` (Java `<= ' '` semantics), `toString`, `hashCode` (Java 31-multiplier), `substring(I)`/`substring(II)`/`subSequence` (real `StringIndexOutOfBoundsException`, index==length legal), `charAt`, `toCharArray`, `compareTo`/`compareToIgnoreCase` (Java char-ordinal ordering), `replace(CC)` and `replace(CharSequence,CharSequence)` (LITERAL — the regex ones are separate), `toUpperCase/toLowerCase` (+Locale overloads, invariant-culture bounded default matching the equalsIgnoreCase convention), `regionMatches` (both), `contentEquals(CharSequence)`, `lastIndexOf` (String/String+from/char/char+from), `intern` (bounded: returns this — no guest string pool), Unicode `codePointAt/Before/Count/offsetByCodePoints` (UTF-16 surrogate aware), regex-backed `split(String)`/`split(String,int)` (Java trailing-empty semantics + limit rules over .NET Regex), `matches` (full-match), `replaceAll` (regex), bounded `format` (static, `%s %d %f %x %X %b %c %n %%` + width/precision/positional `%N$`), `copyValueOf`, `valueOf` (I/Z/C/J/D/F/Object/char[]), `getBytes` (UTF-8 default; explicit charset names honored for UTF-8/ISO-8859-1/US-ASCII, unknown → guest `UnsupportedEncodingException`), `getChars`. Constructors from byte[]/char[] are NOT bound — a guest String is a CLR string (representation change, boundary #40). |
| DEX `static_values` (class_def) | the class_def static_values encoded-array is now PARSED (DexReader) and APPLIED at class-load time before any `<clinit>` (DexInterpreter.EnsureClassInitialized), per the official DEX spec and JLS ordering. AGP emits R$* resource-id fields this way instead of a `<clinit>` sput sequence; without it they silently defaulted to 0 � the root cause of `ARSC_NOT_FOUND: 0x00000000` on SKYNET-ApkInstaller-v1.0-debug.apk. The encoded_value format follows the spec (value_arg = width-1, INT=0x04/LONG=0x06/STRING=0x17/etc.; arrays/annotations fail closed); compact FLOAT/DOUBLE use the spec's right-zero-extension (left-shift (width-size)*8 before bit reinterpretation) and compact CHAR is zero-extended (unsigned), both covered by regression tests; the array covers a PREFIX of static fields in declaration order (spec rule). `sget` now fires a `StaticFieldMissDiagnostic` (host wires it to stderr) when a static field resolves nowhere � the fallback VALUE stays 0/null (real JLS semantics), the "why is it defaulting" path is now observable. |
| `java.util.LinkedHashMap` | migrated to `ApiLayer\Bindings\JavaUtilLinkedHashMapBindings.cs`. INVESTIGATION RESULT: the 3-arg constructor `(int,float,boolean)` is called by `androidx.collection.LruCache.<init>` with accessOrder=true, and NO guest subclass of LinkedHashMap exists and NO `removeEldestEntry` override/reference exists anywhere in the APK (LruCache manages eviction manually via trimToSize, not the protected hook) — so the built scope is the PLAIN ACCESS-ORDER case, with NO removeEldestEntry eviction callback machinery. Dedicated `LinkedHashMapPeer`: private dictionary + null-key sentinel (mirrors HashMapPeer) + a parallel key-order list (mirrors OrderedSetPeer's reasoning). accessOrder=false (default): insertion order, get/put on an existing key does NOT reorder (same as LinkedHashSet). accessOrder=true: ACCESS order — every successful get AND put (including updating an existing key) moves the entry to most-recently-used-last. Views (entrySet/keySet/values) iterate the map's ACTUAL order via the peer's ordered Entries(); the shared Map commons/basic accessors are wired for LinkedHashMap in JavaUtilMapBindings. The float loadFactor/int initialCapacity ctor args are accepted and unused (bounded). |
| `java.util.LinkedHashSet` | migrated to `ApiLayer\Bindings\JavaUtilLinkedHashSetBindings.cs`; ORDER-PRESERVING set semantics via a dedicated `OrderedSetPeer` — a `List<object?>` keeping insertion order plus a linear Contains check before Add for no-duplicate semantics. Deliberately NOT the raw `HashSet<object?>` peer used by CopyOnWriteArraySet (.NET does not reliably guarantee HashSet enumeration order — wrong order would be a latent bug even if unobserved). Real semantics honored: a duplicate add returns false and does NOT move the element (first-insertion order, not most-recent-touch — unlike LinkedHashMap's optional access-order mode); remove then re-add DOES move it to the end (fresh insertion). Probe-confirmed surface: `<init>()V`/`<init>(I)V`/`<init>(Collection)`, `add`, `addAll`, `clear`, `isEmpty`, `iterator`, `remove`, `removeAll` (size/contains not referenced — omitted by discipline). |
| `java.util.ArrayDeque` | migrated to `ApiLayer\Bindings\JavaUtilArrayDequeBindings.cs`; reuses the shared `ListPeer` shape (double-ended ops map directly onto `List<object?>`). Two real contracts honored distinctly, NOT conflated: `getLast`/`remove`/`removeFirst`/`removeLast`/`pop` THROW `NoSuchElementException` on empty (pop is documented as removeFirst), while `peek`/`poll`/`pollFirst` return null on empty; and ArrayDeque does NOT permit null elements (unlike ArrayList) — every add-style method throws `NullPointerException`. Probe-confirmed surface: `<init>()V`, `<init>(Collection)` (copies, rejecting null), `add`/`addFirst`/`addLast`/`offer`/`push` (all NPE on null), `remove()`/`removeFirst`/`removeLast`/`pop` (NSE on empty), `getLast` (NSE on empty), `peek`/`poll`/`pollFirst` (null on empty), `remove(Object)Z` (first occurrence), `size`/`isEmpty`/`clear`/`iterator`, plus the Queue/Deque interface ids for add/offer/poll/peek/pop/push/removeFirst/isEmpty/iterator so interface-typed call sites resolve. NOT built (not referenced): getFirst, peekFirst/peekLast, offerFirst/offerLast, element, removeFirstOccurrence/removeLastOccurrence, descendingIterator, array conversion. The shared `Set.iterator`/`size`/`contains`/`isEmpty` bindings resolve BOTH Set-shaped stores (snapshot MapViews and CopyOnWriteArraySets — real-APK-found bug fixed with a shared backing resolver). |
| `java.util.Collections` | migrated to `ApiLayer\Bindings\JavaUtilCollectionsBindings.cs`. TWO families with genuinely different contracts, deliberately not conflated: (1) `synchronizedMap` simply returns the SAME underlying map object — under the GIL all guest bytecode already executes serialized, no synchronization wrapper needed (real architectural consequence, same reasoning as Lazy/monitor-enter; only synchronizedMap is referenced); (2) `unmodifiableList`/`unmodifiableMap`/`unmodifiableSet` + `emptyList`/`emptyMap`/`emptySet` + `singletonList`/`singletonMap`/`singleton` enforce REAL immutability — wrappers sharing the backing peer whose writes (put/add/set/remove/clear) throw guest `UnsupportedOperationException` while reads delegate (List/Map use an Unmodifiable flag on the shared peer; every Set this runtime produces is immutable, so Set.add/remove/clear always throw). Also probe-confirmed: `sort(List)` (natural ordering for strings/boxed numerics; DexObject tries its guest compareTo) / `sort(List,Comparator)` (Comparator invoked via the guest-functional-interface pattern), `reverse`, `max`/`min` (+Comparator, `NoSuchElementException` on empty), `fill`, `addAll(Collection,Object[])` (ArrayList targets; others throw UOE), `newSetFromMap` (bounded: independent HashSet — the JSONObject add/contains use works; no write-through to the backing map; identity semantics not modeled). Deliberately NOT built (referenced but depend on unmodeled machinery): `binarySearch`/`shuffle` (Comparable/Random), `list(Enumeration)` (Enumeration unmodeled), EMPTY_LIST/EMPTY_MAP/EMPTY_SET static fields (not referenced). |
| `kotlin.Lazy` | `LazyKt.lazy(Function0)Lazy` (the `by lazy {}` delegate), `Lazy.getValue()Object` (computes via the Function0's `invoke()Ljava/lang/Object;` on first access — the same guest-functional-interface invocation pattern Thread uses for Runnable — then caches), `Lazy.isInitialized()Z`. Probe-confirmed shape: NO explicit `LazyThreadSafetyMode` overload and NO `getValue(Lazy,Object,KProperty)` delegate-operator extension are referenced — the real call sites call `Lazy.getValue()` directly, so only those three are bound. No real thread-safety mechanism is built: the GIL serializes all guest bytecode execution, so a plain "compute once and cache" is correct under all three real LazyThreadSafetyMode values — no observable difference exists under this execution model (same reasoning as monitor-enter/AtomicReference). |
| `java.lang.String` (integration bugfixes) | `String.format` now UNBOXES boxed-primitive varargs (`NumericArgument` reuses the BoxedPeer store's TryGet + RawValue) for `%d`/`%f`/`%x`/`%X`/`%c`, and `%s` now dispatches to the guest real `toString()` via `ValueOfObject` (falling back to the bound framework toString for boxed primitives) instead of CLR `ToString()` � found by running the new real `MelyNails.apk`, which crashed with InvalidCastException on a boxed number in a worker-naming `String.format`. Validation now includes MelyNails.apk and SKYNET-ApkInstaller-v1.0-debug.apk alongside SKYNET-FlexGrabber.apk. |`n| `java.util.Map` views + `Map.Entry` | migrated to `ApiLayer\Bindings\JavaUtilMapBindings.cs` as the Map surface grows; `entrySet()`/`keySet()`/`values()` for `HashMap` and `WeakHashMap` (and the `Map` interface ids) return SNAPSHOT views captured at call time (same convention Iterator established for ArrayList) — a Set/Collection-shaped peer supporting `iterator()`/`size()`/`contains()`/`isEmpty()`, so the overwhelmingly common for-each iteration works correctly. The views are deliberately NOT live write-through (mutating the view does not mutate the backing map) — snapshot-only is the honest bounded scope, confirmed sufficient for the probe's usage. `Map.Entry` is `getKey()`/`getValue()` on a snapshot peer; `setValue` is not bound (not referenced by the probe; would need write-through). `HashMapPeer` gained a sentinel-un-translating enumeration accessor so both peer shapes feed the SAME view logic (no per-map duplication); WeakHashMapPeer's public Entries is consumed directly. Probe-confirmed Map commons also bound: `getOrDefault`, `remove(Object,Object)` (only when the value matches), `containsValue`, `isEmpty`, `clear`, `putAll`, `HashMap.equals`/`hashCode` (value-based map equality), plus the basic accessors mirrored on the `Map` interface ids. `Iterator.remove` (referenced by libs) is NOT bound — the snapshot iterator cannot remove from the backing map. |
| `java.lang` boxing (`Boolean`/`Integer`/`Long`/`Short`/`Byte`/`Character`/`Double`/`Float`) | boxed primitives as DexObject typed as the box class + `BoxedPeer` holding the raw value. valueOf caching is the REAL documented JDK contract: `Boolean` ALWAYS the two singletons; `Integer`/`Short`/`Byte`/`Long` cache -128..127; `Character` caches 0..127 (unsigned asymmetry); `Double`/`Float` NEVER cache. Equality is VALUE-based (not identity — unlike Class/Enum). Documented hash codes: Boolean 1231/1237, Integer value, Long `(int)(v^(v>>32))`, Short/Byte/Character value, Float `floatToIntBits`, Double `(int)(bits^(bits>>32))`. Per type: valueOf(primitive) + valueOf(String) where referenced, same-type unboxing (`booleanValue`/`intValue`/`longValue`/`shortValue`/`byteValue`/`charValue`/`doubleValue`/`floatValue`), equals, hashCode (instance + primitive-param static), toString (instance + static, incl. radix 2/8/16 for Integer/Long), parse (parseBoolean → false for non-"true", parseInt/parseLong/parseShort/parseByte/parseDouble/parseFloat with guest `NumberFormatException` on failure), compareTo/compare (incl. real Double/Float NaN ordering and -0.0 < 0.0), Double/Float isNaN/isInfinite; static-field resolver exposes Boolean.TRUE/FALSE and the per-type TYPE class constants. NOT built (referenced by libs, separate features): Integer/Long bit operations (bitCount/highestOneBit/numberOfLeadingZeros/rotateLeft/signum/toBinaryString/decode/getLong), Character's Unicode static predicates (isDigit/isLetter/getType/toUpperCase/...), Double/Float bit methods (doubleToLongBits/floatToIntBits/longBitsToDouble/intBitsToFloat). |
| `java.lang.reflect.Method` (bounded) | minimal reflection surface proven by a real caller-chain + full-APK annotation scan (androidx.lifecycle legacy observer scan — see boundary #43): `Class.getDeclaredMethods()[Method]` (guest DirectMethods+VirtualMethods, excluding `<init>`/`<clinit>` per the real JDK contract; EMPTY array for framework types — no guest method model), `Class.getInterfaces()[Class]` (canonical objects from `DexClass.Interfaces`), `Method.getName()String`, `Method.getAnnotation(Class)Annotation` — ALWAYS null (this runtime does not model annotation metadata at all; DEX annotation parsing is not implemented, "not present" is the only honest answer — confirmed correct for the observed path where no class declares `@OnLifecycleEvent`). Fresh `Method` objects per call (real Java does not guarantee Method reference identity across reflective calls — no canonical cache). NO `Method.invoke` (provably unreachable on this path), NO `getModifiers`/`Modifier`, NO `getClassLoader`/`getMethods`/`getDeclaredFields`, NO real Annotation model. |
| `java.util.concurrent.ConcurrentHashMap` | own file (`JavaUtilConcurrentBindings.cs`) - real contract VERIFIED against the OpenJDK source (Android's libcore imports this class near-verbatim, not a reimplementation): unlike HashMap, a null key OR null value throws NullPointerException on put/putIfAbsent/get/containsValue; putIfAbsent returns the EXISTING value without overwriting when the key is present; all 5 constructors built, with IllegalArgumentException for negative capacity / nonpositive loadFactor or concurrencyLevel. No internal lock-striping/CAS is modeled - the GIL serializes all guest bytecode, so a plain unsynchronized dictionary is behaviorally identical (same reasoning as AtomicReference/Collections.synchronizedMap/kotlin.Lazy). Probe of MelyNails.apk: `<init>`, `get`, `isEmpty`, `keySet`, `put`, `putIfAbsent`, `remove(Object)`, `size`, `containsKey`, `containsValue`, `clear`, `putAll`, `mappingCount` - views reuse the shared Map-view machinery (`JavaUtilMapBindings.MapPairsFor`). |
| `kotlin.text.StringsKt` | Kotlin String extension functions (the class is guest-defined in the dex but R8 shrunk it — confirmed GAP via the interpreter's own FindMethodExact, so these are framework bindings). Bound core subset that mirrors java.lang.String and DELEGATES to the same logic: `replace(String,String,String,Z)`/`replace$default` + `replace(String,C,C,Z)`, `contains(CharSequence,C/CharSequence,Z)`/`$default`, `startsWith`/`endsWith` (CharSequence/String receivers, char + String, with fromIndex; `$default`), `indexOf`/`lastIndexOf` (char/String + fromIndex + ignoreCase; `$default`), `equals(String,String,Z)`, `compareTo(String,String,Z)`, `trim`/`trimStart`/`trimEnd` (CharSequence, same `<= ' '` convention as Java trim), `isBlank`, `take`/`drop`, `padStart`/`padEnd`, `repeat`, `split(CharSequence,[C,Z,I)List`/`$default` (char-array delimiters, Java limit semantics; bounded remainder join for limit>0 with multiple delimiters), `getLastIndex`, `toCharArray(String,I,I)`. The Kotlin `$default` wrapper convention (bitmask + trailing Object marker; set bit = use the method default) is implemented once in `RegisterWithMask` and reused by every wrapper. Deliberately NOT bound: `getIndices` (returns kotlin.ranges.IntRange — no representation), `toIntOrNull`/`toLongOrNull` (return Integer/Long — no boxing model; representation boundary if reached), and the ~150 remaining StringsKt refs (substringBefore/After family, removePrefix/Suffix/Surrounding, capitalize, lines/lineSequence, windowed, replaceBefore/After, trimIndent/Margin, findAnyOf, sequences, radix overloads, etc.). kotlin.reflect / kotlin.sequences / kotlin.time / annotation accessors are all out of scope (see boundary #41). |

Framework lookup is exact and bounded through `Activity → ContextThemeWrapper → ContextWrapper → Context` and `Bundle → BaseBundle`, plus DEX-defined superclass chains. Bundle, Intent, StringBuilder, Toast, launcher Intent, and application Context state live in typed per-session peer stores rather than guest fields. Per-type peer quotas are configurable and fail closed; hosted-session termination deterministically clears every peer store. The launcher Intent is stable per session with `android.intent.action.MAIN`; the application Context is a distinct stable object.

Logs use finite Android priorities 2–7 with configurable minimum priority and retain session/package/Activity/invocation attribution. Text Toasts enforce configurable text, queue, duration, and per-minute limits. The Windows host composites Toast text into the same retained child-HWND surface as Android content, avoiding WPF/Win32 airspace conflicts, and uses a generation-checked `DispatcherTimer`.

`String.equalsIgnoreCase` decodes valid UTF-16 surrogate pairs and compares Unicode code points using Java's upper-then-lower algorithm; unpaired surrogates remain deterministic individual code units. Its simple-case mapping is pinned to Microsoft OpenJDK 17.0.20 / Java SE 17 (`Character`, Unicode 13.0). A bounded override set covers 12 BMP mappings and the compact Vithkuqi range where the newer .NET 10 Unicode profile differs.

Semantic references: [Android Activity](https://developer.android.com/reference/android/app/Activity), [Context](https://developer.android.com/reference/android/content/Context), [BaseBundle](https://developer.android.com/reference/android/os/BaseBundle), [Intent](https://developer.android.com/reference/android/content/Intent), [Log](https://developer.android.com/reference/android/util/Log), [TextUtils](https://developer.android.com/reference/android/text/TextUtils), [Color](https://developer.android.com/reference/android/graphics/Color), [Toast](https://developer.android.com/reference/android/widget/Toast), and [Microsoft WPF live-region guidance](https://learn.microsoft.com/en-us/accessibility-tools-docs/items/wpf/text_livesetting).

API identity is only class descriptor + method name + full method descriptor. Invoke kind is call-site metadata, never part of the registry key. Missing APIs emit `Unimplemented` and throw `AndroidApiNotImplementedException`; there is no silent default.

### Interpreter correctness fixes and UI-layer bindings batch (validated against a real APK reaching full onResume)

Alongside the ConcurrentHashMap work above, this batch also fixed real DEX-interpreter bugs and closed a large UI-layer API surface. Scope grew beyond the original single-class task during an unsupervised implementation session; every file was independently reviewed, one broken/incomplete binding was completed (`TypedArrayObject`, a missing canonical singleton property), a small nullable-annotation inconsistency was cleaned up, and the whole result was re-validated (build clean, full test suite green, all three real APKs re-run) before being kept - it is recorded here rather than discarded because it is correct, tested, and demonstrably necessary: `SKYNET-ApkInstaller-v1.0-debug.apk` now runs its **complete visible lifecycle** (`onCreate` -> layout inflation -> foreground service -> battery-optimization check -> `onResume`) with zero errors, which was not possible before it.

**Interpreter fixes (`DexInterpreter.cs`):**
- `instance-of` now uses real assignability (superclass/interface chain) instead of exact-descriptor comparison, and supports array receivers - `instanceof Activity` on a subclass, and `instanceof` on arrays, were both silently wrong before.
- `iget`/`iput`/`iget-wide`/`iput-wide` now resolve/mirror the same logical field slot across every superclass owner key in the field's chain. Real Java resolves an instance field to its DECLARING class, but DEX compiles the field reference against the compile-time owner - a Kotlin R8-shrunk `FunctionReferenceImpl` lambda subclass reads `Subclass->receiver` while the parent constructor wrote `Parent->receiver`, silently returning the default value instead of the real one. Known tradeoff: a subclass that deliberately SHADOWS a superclass field with the same (name,type) would incorrectly share one slot; not observed on any validated APK.
- `invoke-interface` now resolves Java 8 interface default methods (walks the interface's own declared methods and super-interfaces, cycle-safe) instead of only guest-class/superclass bodies.
- `SuperclassOf` now returns `Ljava/lang/Object;` for array descriptors (arrays are Object subtypes for assignability purposes).

**UI/system-service API surface (`AndroidApiBindings.cs`, `AndroidSystemServices.cs`, `Ui/*`):** `LayoutInflater` (`from`, three `inflate` overloads, `getFactory`/`setFactory2`), `Window` (`getWindow`, `setContentView` x2, `getDecorView`, callback/attributes/status-bar-color), `Resources` (`getString`/`getColor`/`getIdentifier`/`getConfiguration`/`LocaleList`), a bounded `TypedArray` facade for appcompat theme setup (empty-array semantics - `getIndexCount`, `hasValue`, typed getters, `recycle`), `Activity` extensions (`getApplication`, `runOnUiThread`, `registerActivityLifecycleCallbacks`, fragment-manager stubs), a `PackageManager` facade (`hasSystemFeature`, `checkPermission`, package/application info), `PowerManager` (`isIgnoringBatteryOptimizations` - gated behind the existing `--grant-power` capability, same as clipboard/network), `Context` service starters (`startService`/`startForegroundService`/`bindService`/`getSharedPreferences` - the last one is the next open boundary), `ThreadLocalRandom`, `AtomicBoolean`, plain `HashSet`/`Collection` operations dispatching across whichever peer store actually backs the receiver (same multi-store resolver pattern the `CopyOnWriteArraySet` regression fix established), `View`/`ViewGroup` additions, `Object.clone()`/array `clone()`, and `Class.getMethod`/`getDeclaredMethod`/`newInstance` (bounded reflection, reusing the existing fail-closed conventions). The layout inflater gained `<include>`, `<merge>`, `CardView`, `EditText`, `ProgressBar`, `AppCompatButton`, `FrameLayout`, and `ViewStubCompat`.

## Windows CLI

```powershell
AndroidRuntime.WindowsHost.exe <apk> [--auto-close-ms <100..600000>] [--trace <path>] [--grant-clipboard-read] [--grant-clipboard-write] [--grant-network-state]
```

- Missing/invalid arguments return exit code `2`.
- Cancellation returns `3`.
- Runtime/host failures return `1`.
- A successful close returns `0`.
- `--auto-close-ms` is intended for bounded smoke automation.
- Trace output is normalized with Windows case-insensitive path semantics and rejected before launch when it aliases the input APK.
- Clipboard read, clipboard write, and network-state capabilities default to deny and require their separate explicit grant flags. Duplicate or unknown grants are usage errors.
- Connectivity additionally requires a direct manifest `android.permission.ACCESS_NETWORK_STATE` declaration. Unknown services and unavailable adapters return null; a denied lookup/operation throws a catchable guest `SecurityException`.

Run the checked smoke harness after building:

```powershell
.\scripts\smoke-windows-host.ps1
```

It verifies a non-zero HWND, the DEX-provided title `RuntimeProbe DEX`, bounded auto-close, exit code zero, and parses every JSON-lines trace event under `artifacts/smoke/`. It requires one session identity, one `Requested`-to-terminal pair per invocation UUID, and a completed `setTitle` binding. Cleanup uses APIs compatible with Windows PowerShell 5.1.

## Trace model

Every API attempt produces correlated events from this set: `Requested`, `Completed`, `Unimplemented`, `Failed`, or `Cancelled`; guest throws additionally emit a redacted `GuestThrew` event. Fields include:

- session sequence and invocation UUID;
- exact caller and DEX program counter;
- requested and resolved API identity;
- invoke kind;
- summarized/redacted arguments;
- managed thread and Android main-lane flag;
- cancellation token context;
- session, package, and Activity identity;
- terminal error type when applicable.

Trace sinks are non-authoritative and non-throwing. `AndroidApiTraceBuffer` is bounded and reports `DroppedCount` when older events are evicted.

## Honest limits

This is a production-shaped boundary, not a production-complete Android implementation. It does **not** claim CTS compatibility, the Android View toolkit, resources, Binder, JNI, complete Android permissions, a complete Looper/Handler, task/back-stack semantics, or configuration changes. Wide execution is intentionally limited to the documented long/double register, field, array, conversion, comparison, and arithmetic opcodes; it is not a full verifier. DEX exception support parses bounded, non-overlapping `try_item`/encoded-handler tables, runs ordered typed or catch-all handlers, preserves throwable identity across frame unwinding, and exposes sanitized uncaught errors. It omits suppressed exceptions, host stack exposure, `printStackTrace`, and guest stack-trace mutation. Cancellation, malformed DEX, quotas, and unexpected host/backend failures remain non-catchable control-plane errors. Clipboard support is plain Unicode text only, focus-gated for reads, bounded, and has no listeners/rich formats/URI items. The default Windows clipboard adapter uses a dedicated STA Dispatcher, persistence copy, bounded retry/deadline, and is tested through an injected non-global backend; automated smoke never reads or mutates the user's global clipboard. Connectivity is one immutable default-network snapshot only: the coarse Windows adapter never performs HTTP probes, never exposes names/IP/GUID/SSID, reports VALIDATED false and metering unavailable rather than fabricating them, and omits callbacks/all-networks. Windows uptime uses unbiased interrupt time (precise when exported, documented fallback otherwise), elapsed realtime uses `GetTickCount64`, and elapsed nanos uses the monotonic performance counter. Deliberately deferred APIs include Log Throwable overloads, Parcelable/Parcel/Uri/component Intent behavior, rich clipboard/listeners, network callbacks/details, Toast resource IDs, and custom Toast Views.

Hardening invariants are fail-closed: array opcodes must match their exact primitive/reference component category, primitive arrays are invariant, and reference arrays use recursive Java covariance across dimensions and DEX/framework class hierarchies. Every array is assignable to `Object`, `Cloneable`, and `Serializable`; mixed dimensions still follow component assignability rather than shape guessing. Array arguments, casts, stores, and returns use the same descriptor rules. `move-result*` must immediately follow the producing invoke with the matching word/reference/wide kind. Exception-table padding must be zero and bounded ULEB32/SLEB32 encodings reject invalid fifth-byte overflow/sign extension. Manifest permissions are immutable defensive snapshots. Every service Context, manager, ClipData/Item, Network, and NetworkCapabilities peer is owned by exactly one runtime session; cross-session objects are rejected before backend access. Clipboard and connectivity calls carry the invocation cancellation token through the adapter boundary.

CLR array values crossing an API binding are converted through one recursive descriptor matrix covering `Z/B/S/C/I/J/F/D`, `String`, `Object`, generic `DexObject`/`DexArray` wrappers, and jagged rank-one arrays. Rectangular CLR arrays (`Array.Rank != 1`) are always rejected because Java multidimensional arrays are arrays of array references, not rectangular storage. The resulting Java descriptor is then evaluated by the same covariance and primitive-invariance rules; CLR shape never bypasses guest assignability.

## Work-unit rollback boundaries

1. **API boundary**: revert `ApiLayer/AndroidApiRegistry.cs`, interpreter call-site context/cancellation changes, and `AndroidApiBoundaryTests.cs` together.
2. **Host/lifetime**: remove `Hosting/`, `AndroidRuntime.WindowsHost/`, its test project, solution entries, and smoke script. Core APK/DEX/lifecycle execution remains.
3. **Bindings/fixture**: remove `AndroidApiBindings.cs`, restore the fixture before `setTitle`/`Log.i`, and remove hosted integration tests. The observable registry boundary and host ports remain independently usable.
4. **Verification hardening**: revert CLI collision validation, close-triggered termination, contextual log entries, the `UnimplementedApiProbe.apk` variant, and smoke JSON validation together without affecting parsing or lifecycle semantics.
5. **Dispatch and semantic hardening**: revert invoke-kind dispatch, typed guest arithmetic/null errors, exact return assignability, peer quotas/cleanup, and Toast acceptance accounting together; the earlier binding portfolio remains but loses these correctness guarantees.
6. **Wide values and clocks**: revert `DexArray.cs`, wide opcode/frame changes in `DexInterpreter.cs`, `IAndroidClock`/`WindowsAndroidClock`, SystemClock and Bundle-long bindings, and their tests together.
7. **Portable framework portfolio**: revert `AndroidFrameworkState.cs`, the expanded bindings, generalized lookup/reference branches, Toast ports/WPF overlay, and portfolio fixture/tests together. The earlier lifecycle, title, and single Log.i slice remains independently recoverable.
8. **Guest exceptions**: revert DEX try/handler models and parsing, guest throwable/carrier/public exception types, `throw`/`move-exception` execution, Throwable bindings/hierarchy, `GuestThrew` tracing, `ExceptionProbe.apk`, and focused tests together. Wide execution and the earlier hosted lifecycle remain independently usable.
9. **Governed services**: revert manifest permissions, capability/audit/clipboard/connectivity ports, `AndroidSystemServices.cs`, Windows adapters and CLI grant flags, ServicesProbe variants, tests, and service documentation together. Guest exceptions, lifecycle, and earlier bindings remain independent.
10. **Runtime hardening correction**: revert typed array enforcement, immediate `move-result*` tracking, strict code-item padding/LEB parsing, immutable permission snapshots, service peer ownership/cancellation, retryable clipboard dispatcher shutdown, and their focused tests together. This does not remove the WU1-WU5 feature surface.
11. **Headless Android UI bindings**: revert `Ui/AndroidLayoutInflater.cs`, `Ui/AndroidUiSession.cs`, the View peer/binding/interpreter additions, `UiProbe` callback fixture, and `AndroidUiRuntimeIntegrationTests.cs` together. Resource parsing and the retained scene foundation remain independently usable.
12. **Windows retained surface**: revert AndroidRuntime.WindowsHost/WindowsRetainedRenderer.cs, AndroidHwndRenderSurface.cs, the IAndroidUiSurfaceHost attach port, and their Windows tests together. Headless Android UI remains independently usable.
13. **Multidex APK support**: revert `LoadedApk.ClassesDexFiles`/`ApkLoader` numeric collection and secondary-DEX quotas, `DexFileSet`, the `DexEncodedMethod.OwningDex` ownership stamp, `DexInterpreter` merged lookup and frame-local pool resolution, `AndroidAppRuntime` per-file verify/parse, the multidex tests, and this entry together. Single-dex loading and dispatch remain independently usable.
14. **ARSC Modified UTF-8**: revert the `AndroidBinaryFormat.ReadUtf8` CESU-8/Modified-UTF-8 decode (surrogate-half acceptance, 0xC0 0x80 NUL, fail-closed malformed-sequence handling) and `AndroidBinaryFormatUtf8Tests.cs` together. Resource-table, manifest, and layout parsing remain independently usable; genuinely malformed strings still fail closed in either version.
15. **ARSC complex entries skip**: revert the `AndroidResourceTable.ParseType` complex-entry skip (FLAG_COMPLEX entries continue instead of throwing, global entry quota counts skipped entries) and the updated `AndroidResourceTests` complex-entry case together. Truncation/malformed fail-closed behavior and the honest ARSC_NOT_FOUND lookup boundary remain in either version.
16. **ARSC typed values (fraction)**: revert the `AndroidFractionUnit`/`AndroidFraction`/`AndroidResourceValueKind.Fraction` additions, the `FromBinary` 0x06 wiring, the shared `DecodeComplexMantissa` refactor (same multipliers as the original `DecodeDimension`: {256, 32768, 8388608, 2147483648}, verified against AOSP fixed-point formats 23p0/16p7/8p15/0p23), and the fraction/dimension tests together. Dynamic references 0x07/0x08 were investigated (probed the real SKYNET resources.arsc: 0 ResTable_lib_header 0x0203 chunks, 0 dynamic values) and remain honestly throwing ARSC_UNSUPPORTED — not silently approximated.
17. **DEX monitor-enter/monitor-exit**: revert the `DexOpcodeTable` F11x metadata for 0x1d/0x1e (format switch + supported range), the `DexInterpreter` no-op dispatch cases (null-checked, deliberately no monitor state for the single-threaded lane), and the verifier/interpreter tests together. Synchronized methods remain un-executable in the reverted state, and the verifier metadata test returns to excluding 0x1d/0x1e.
18. **DEX packed/sparse-switch**: revert the `DexOpcodeTable` F31 metadata for 0x2b/0x2c, the `DexVerifier.ValidateFlow` switch branch-target + payload-kind confirmation, the `DexInterpreter` switch dispatch and payload parsers (packed 4+2·size units, sparse 2+4·size units, targets relative to the switch pc, sparse keys verified ascending), the minimal-DEX verifier builder/tests, and the interpreter switch tests together. Switch statements remain un-executable in the reverted state.
19. **DEX filled-new-array**: revert the `DexOpcodeTable` F35c/F3rc metadata for 0x24/0x25, the `DexVerifier` type-pool index check for them, the `DexInterpreter` array-allocation dispatch cases (type-id operand, length = argument count, DexArray fill, lastInvokeResult publication), and the filled-new-array tests together. Fresh-array literals remain un-executable in the reverted state.
20. **DEX float arithmetic**: revert the `DexOpcodeTable` F23x/F12x metadata for cmpl/cmpg-float (0x2d/0x2e), float 23x (0xa6..0xaa) and float /2addr (0xc6..0xca), the `DexInterpreter` FloatBinOp helper, the three dispatch cases, the float arg/return int-bit boundary handling in `LoadArguments`/`UnwrapRootReturn`, and the float tests together. Float-typed methods remain un-executable in the reverted state.
21. **DEX neg-float**: revert the `DexOpcodeTable` F12x metadata for 0x7f, the `DexInterpreter` neg-float dispatch case (single-width sign-bit flip), and its float test together. Unary float negation remains un-executable in the reverted state.
22. **DEX fill-array-data**: revert the `DexOpcodeTable` F31 metadata for 0x26, the `DexVerifier.ValidateFlow` kind-3 payload confirmation, the `DexInterpreter` dispatch case + `FillArrayFromPayload`/`ReadPackedBytes` helpers (element-width/component match, size <= Length, Set/SetWide by component), and the fill-array-data tests together. Array literals with constant initializers remain un-executable in the reverted state.
23. **DEX iput-short**: revert the `DexOpcodeTable` F22c metadata for 0x5f (instance-field-put family now complete) and the iput-short round-trip test together. The raw no-truncation field store behavior is unchanged from the rest of the iget/iput family.
24. **API Object.<init>**: revert the `RegisterVoid(builder, "Ljava/lang/Object;", "<init>", "()V")` binding, the README portfolio `Object` row, and the plain-class construction test together. Plain DEX classes extending java.lang.Object directly fail at their first super() call in the reverted state.
25. **DEX interface assignability**: revert the `DexClass.Interfaces` field + `DexReader` interfaces_off parsing and `ReadTypeList` extraction, the `IsAssignable` interface-walk extension (4-arg overload, `InterfaceClosureContains` with cycle/depth guards), the `DexInterpreter.InterfacesOf` wiring, and the interface-assignability tests together. Guest interface-typed arguments remain rejected in the reverted state.
26. **API AtomicReference**: revert the `AtomicReferencePeer` class, the `AndroidPeerLimits.MaxAtomicReferences` + state store/property/Clear touch points, the five `AndroidApiBindings` bindings, the README portfolio row, and the atomic-reference tests together. AtomicReference calls remain unbound in the reverted state.
27. **API WeakHashMap**: revert the `WeakHashMapPeer` class, the `AndroidPeerLimits.MaxWeakHashMaps` + state store/property/Clear touch points, the six `AndroidApiBindings` bindings, the README portfolio row, and the weak-hash-map tests together. WeakHashMap calls remain unbound in the reverted state.
28. **API HashMap**: revert the `HashMapPeer` class (null-key sentinel, distinct from WeakHashMapPeer), the `AndroidPeerLimits.MaxHashMaps` + state store/property/Clear touch points, the six `AndroidApiBindings` bindings, the README portfolio row, and the hash-map tests together. HashMap calls remain unbound in the reverted state.
29. **API ArrayList**: revert the `ArrayListPeer` class, the `AndroidPeerLimits.MaxArrayLists` + state store/property/Clear touch points, the seven `AndroidApiBindings` bindings, the README portfolio row, and the array-list tests together. ArrayList calls remain unbound in the reverted state.
30. **API WeakReference**: revert the `WeakReferencePeer` class, the `AndroidPeerLimits.MaxWeakReferences` + state store/property/Clear touch points, the three `AndroidApiBindings` bindings, the README portfolio row, and the weak-reference tests together. WeakReference calls remain unbound in the reverted state.
31. **API CopyOnWriteArraySet**: revert the `AndroidPeerLimits.MaxCopyOnWriteArraySets` + state store/property/Clear touch points, the two `AndroidApiBindings` bindings (using `HashSet<object?>` directly as the peer value), the README portfolio row, and the copy-on-write tests together. CopyOnWriteArraySet calls remain unbound in the reverted state.
32. **Framework interface hierarchy**: revert the `AndroidFrameworkHierarchy.Interfaces` table + `InterfacesOf` (Set/List/Map/Collection edges for the bound collection classes), and the `DexInterpreter.InterfacesOf` guest+framework merge together. Interface-typed assignments of framework collection classes fail at check-cast in the reverted state.
33. **List completion + Iterator**: revert the `ListPeer` rename/sharing, `IteratorPeer` snapshot mechanism, `AndroidPeerLimits.MaxIterators`/`MaxCopyOnWriteArrayLists` + state store/property/Clear touch points, the completed ArrayList surface, the full CopyOnWriteArrayList surface, the Iterator bindings, the NoSuchElementException hierarchy entry, the README portfolio rows, and the list/iterator tests together. The list and iterator APIs return to their earlier partial/unbound states.
34. **DEX class initialization**: revert the `EnsureClassInitialized` helper, the per-session `_classInitState` tracking, the three trigger points (new-instance, sget/sput, invoke-static), and the class-initialization tests together. Guest `<clinit>()V` methods return to never running, and static fields stay at defaults in the reverted state.
35. **API Enum + Bindings convention**: revert `EnumPeer`/`MaxEnums` + state touch points, the `ApiLayer\Bindings\JavaLangBindings.cs` file (moved `Object.<init>` plus the Enum bindings), the `CreateBuilder` wiring, the README portfolio `Enum` row, and the enum tests together. Enum calls return to unbound and Object.<init> moves back into the monolith.
36. **API AtomicInteger + atomic package file**: revert `AtomicIntegerPeer`/`MaxAtomicIntegers` + state touch points, `ApiLayer\Bindings\JavaUtilConcurrentAtomicBindings.cs` (moved AtomicReference + new AtomicInteger), the `CreateBuilder` wiring, the README portfolio `AtomicInteger` row, and the atomic-integer tests together. AtomicInteger calls return to unbound and AtomicReference moves back into the monolith.
37. **Real concurrency foundation (GIL + Thread + real monitor)**: revert `AndroidGil`, the lane's Gil property, `ThreadPeer`/`MaxThreads` + state touch points, `JavaLangThreadBindings.cs`, the interpreter's Gil field/entry-point acquisition, the real monitor-enter/exit (0x1d/0x1e via CLR Monitor), the class-init cross-thread blocking fix, the `AndroidAppRuntime` GIL/AttachInterpreter/main-thread wiring, the `Parents` InterruptedException/IllegalThreadStateException entries, the README architecture/Thread-row changes, and the concurrency tests together. A revert returns the runtime to the single-lane model (monitors back to no-op, class-init to same-thread-skip) — everything built on real threading later (Handler/Looper, ExecutorService) would also need reverting. Note: `ApiLayer\AndroidApiBindings.cs` is a decompiled restoration (nullability analysis disabled, comment at the file head); its functional behavior is validated by the full suite.
38. **ExecutorService + Handler/Looper (Parts 3+4)**: revert `Hosting\AndroidMessageQueue.cs`, `ApiLayer\Bindings\JavaUtilConcurrentExecutorBindings.cs` (Executors/ExecutorService/Future/FutureTask/TimeUnit + the real pool machinery: `ExecutorServicePeer`, worker loops, guest `ThreadFactory` dispatch), `ApiLayer\Bindings\AndroidOsHandlerBindings.cs` (Handler/Looper + `LooperPeer`/`HandlerPeer`), the new peer stores/quotas (`ExecutorServices`/`Futures`/`Loopers`/`Handlers`), the `Lane` property + `AndroidExecutionLane.TryPost`, the interpreter's `FrameworkStaticField` hook + `InvokeFrameworkExact` + structural Runnable/Callable/ThreadFactory assignability, the `AndroidFrameworkHierarchy` Parents (RejectedExecutionException/CancellationException/TimeoutException/ExecutionException) and Interfaces (ThreadPoolExecutor→ExecutorService→Executor, FutureTask→Future/Runnable) entries, the `AndroidApiRegistry` KnownStaticShape entries + interface-aware default assignability, the AndroidAppRuntime `Lane` wiring, the README portfolio/boundary changes, and `ConcurrencyExecutorTests.cs` together. Revert brings the runtime back to the pre-Executors state: pools/looper semantics unbound, `TimeUnit.SECONDS` sget returning null, `Handler` calls unimplemented. Explicit scope boundaries of this work unit: NO `ScheduledExecutorService`/`CompletableFuture`/`ForkJoinPool` (referenced by SKYNET's AsyncManager/OffersManager but explicitly out of scope — next boundary), NO `Message`/`sendMessage` Handler surface (only the Runnable post family), NO `AsyncTask`/`HandlerThread`/`CountDownLatch`/`ThreadPoolExecutor` direct constructor (all referenced by bundled libs but not executed by the target slice). The `newCachedThreadPool` bound max (32) and 60s idle keepalive are deliberate fail-closed bounds; `Future.cancel(mayInterruptIfRunning)` is best-effort cooperative (CLR `Thread.Interrupt`), not a hard kill. **Validated against the real SKYNET-FlexGrabber.apk**: the previous `Executors.newFixedThreadPool` boundary is gone (the pool is created and starts real workers); the run now progresses further into guest construction and stops at the next unimplemented API: `Ljava/lang/Object;->getClass()Ljava/lang/Class;` (a generic Object method reached during Activity construction — needs a minimal Class model, a separate work unit). `Looper.getThread()` was added during validation (kotlinx-coroutines binds it during construction).
39. **java.lang.Class**: revert the const-class representation fix (DexInterpreter 0x1c now produces a `DexObject` typed `Ljava/lang/Class;` with a `ClassPeer(RepresentedDescriptor)` instead of a DexObject typed as the represented class — the old shape could never resolve `Class.getName()` through virtual dispatch), the canonical descriptor→Class cache + `ClassPeer`/`PackagePeer` in `AndroidFrameworkState` (`EnsureClassObject`/`EnsurePackageObject`; bounded by the loaded type universe, deliberately no quota store), the interpreter hooks (`ClassObjectProvider` wired in AttachInterpreter, `RuntimeDescriptorOf`, `IsGuestTypeAssignable`, `SuperclassDescriptorOf`, `EnumConstantsOf`), the `JavaLangBindings` Class/Package bindings + `Object.getClass()` + the previously-deferred `Enum.getDeclaringClass`/`Enum.valueOf(Class,String)`, the KnownStaticShape `Enum.valueOf` entry, the README portfolio/boundary changes, and `ClassTests.cs` together. Revert brings back the old behavior: const-class objects typed as the represented class (getName resolves nowhere), `getClass` unimplemented, and the three deferred Enum call sites unbound again. SKYNET method-table probe result: in-scope methods reached by the real run were getName/getSimpleName/getSuperclass/isInstance/isAssignableFrom/hashCode/toString/getClass plus getCanonicalName and getPackage (both added during validation — the run executes them); `Enum.getDeclaringClass`/`Enum.valueOf` are referenced AND now bound. Explicitly NOT built (referenced by bundled libs but reflection/loading scope, next boundary if ever executed): `Class.forName` (both overloads), `newInstance`, `getMethods/getDeclaredMethods/getMethod/getDeclaredMethod`, `getFields/getField/getDeclaredFields/getDeclaredField`, `getConstructors/getConstructor/getDeclaredConstructor`, `getModifiers`, `getComponentType`, `isArray/isPrimitive`, `cast/asSubclass`, `getClassLoader`, `getEnumConstants`, `getInterfaces`, `getGenericInterfaces`, `getTypeParameters`, `getEnclosingConstructor/Method`, `isAnonymousClass/isLocalClass/isMemberClass`, `getAnnotation`, `getResourceAsStream`. Documented bounded simplifications: `getCanonicalName` does not detect inner-class canonicalization; `getSuperclass` returns null for arrays/primitives (real: arrays→Object); `toString` always prefixes "class " (interface detection not built for this cosmetic case). **Validated against the real APK**: the `Object.getClass` boundary is gone — the run now progresses through Class name/package access and stops at the next unimplemented API: `Ljava/lang/String;->substring(I)Ljava/lang/String;` (a java.lang.String method, separate work unit).
40. **java.lang.String completion**: revert `ApiLayer\Bindings\JavaLangStringBindings.cs` (the migrated + expanded String group), the AndroidApiBindings changes (helpers made internal, `RegisterStrings` moved out, registration call replaced), the `StringIndexOutOfBoundsException`/`UnsupportedEncodingException` hierarchy entries, the KnownStaticShape `String.format` entry, the README portfolio/boundary changes, and `StringContractTests.cs` together. Revert returns String to the partial monolith surface (no substring/charAt/compareTo/replace/split/matches/format/getBytes/etc.). SKYNET method-table probe result (full referenced list, `Ljava/lang/String;`): all of the existing surface, plus substring(I)/substring(II), charAt, toCharArray, compareTo, compareToIgnoreCase, replace(CC)/replace(CharSequence), toUpperCase/toLowerCase (+Locale), startsWith(String,I), regionMatches (both), contentEquals (CharSequence/StringBuffer — StringBuffer variant not bound, no StringBuffer model), lastIndexOf (4 overloads), indexOf (char/char+from), intern, codePointAt/Before/Count/offsetByCodePoints, split(String)/split(String,I), matches, replaceAll, format (both), copyValueOf, valueOf (J/D/F/Object/char[]), getBytes (3), getChars, subSequence, and the constructors `<init>([C)V/<init>([CII)V/<init>([B)V/<init>([BII)V/<init>([B...,Charset).../<init>(StringBuilder/StringBuffer)` — the constructors are NOT bound (a guest String is a CLR string, not a DexObject; constructing one in a binding cannot yield a usable String — reported as a representation-change boundary if the real run ever executes them). Documented bounded choices: no-arg case conversions use the invariant culture (matching the equalsIgnoreCase Unicode-pinning convention); `split`/`matches`/`replaceAll` use .NET Regex (same Perl-derived core syntax; Java dialect differences are not silently resolved — flag on hit); `format` covers `%s %d %f %x %X %b %c %n %%` + basic width/precision/positional, NOT a full printf engine; `intern` returns `this` (no guest string pool); `getBytes(String)` honors UTF-8/ISO-8859-1/US-ASCII and throws guest `UnsupportedEncodingException` for unknown names; `valueOf(D/F)` uses .NET "R" round-trip formatting (close to Java's toString but exponent notation can differ). **Validated against the real APK**: the `String.substring` boundary is gone — the run now progresses through String methods and stops at the next unimplemented API: `Lkotlin/text/StringsKt;->replace$default(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;ZILjava/lang/Object;)Ljava/lang/String;` (a kotlin.text stdlib static method with default-arg synthesis — separate work unit, not java.lang.String).
41. **kotlin.text.StringsKt**: revert `ApiLayer\Bindings\KotlinTextStringsKtBindings.cs`, the `RegisterWithMask` $default machinery, the JavaLangStringBindings helpers made internal for delegation (Substring/JavaCompareTo/JavaCompareToIgnoreCase/ReplaceLiteral), the CreateBuilder registration call, the KnownStaticShape `Class.forName` hygiene entry, the README portfolio/boundary changes, and `KotlinStringsKtTests.cs` together. Revert returns all StringsKt calls to unimplemented. Pre-scope verification (per the orchestrator's own investigation): `Lkotlin/text/StringsKt;` IS guest-defined in SKYNET's dex but R8 shrunk it — confirmed with the interpreter's own `DexFileSet.FindMethodExact` that `replace$default` and every other method below is a GAP (genuine framework-binding gaps, not a multidex resolution bug). Bound core subset (delegates to the Java String logic): replace ×2 + $default, contains ×2 + $default, startsWith ×4 + $default, endsWith ×3 + $default, indexOf ×2 + $default, lastIndexOf ×2 + $default, equals, compareTo, trim/trimStart/trimEnd, isBlank, take ×2, drop, padStart ×2, padEnd ×2, repeat, split(CharSequence,[C,Z,I) + $default (returns a guest List), getLastIndex, toCharArray(String,I,I). Deliberately NOT bound with reasoning: getIndices (returns kotlin.ranges.IntRange — no representation in this runtime), toIntOrNull/toLongOrNull (return Integer/Long — this runtime has no boxing model, a binding cannot produce a usable boxed value; representation boundary if the run reaches them), and the ~150 remaining StringsKt references (substringBefore/After family, removePrefix/Suffix/Surrounding, capitalize, lines/lineSequence, windowed, replaceBefore/After, trimIndent/trimMargin, findAnyOf/findLastAnyOf, indexOfAny/lastIndexOfAny, commonPrefixWith/Suffix, reversed, slice, random, sequences, radix overloads, etc.). Explicitly out of scope: kotlin.reflect.* (real reflection — no reflection model, consistent with Class), kotlin.sequences.* (~40 methods, lazy sequence API, not confirmed), kotlin.time.* (not confirmed), and annotation-class accessors (only meaningful via reflection this runtime doesn't have — almost certainly dead). Bounded choices: ignoreCase=true for replace uses .NET Regex with CASE_INSENSITIVE over the quoted pattern (real Kotlin behavior); indexOf/contains/startsWith ignoreCase use the established JavaEqualsIgnoreCase mapping; split's limit>0 remainder with MULTIPLE delimiters joins with the first delimiter (exact for the single-delimiter common case). The `$default` convention (bitmask + trailing null Object marker; a set bit = use the method default) is implemented once in RegisterWithMask — every optional param in this surface defaults to 0/false, so substitution is uniform; the wrapper descriptor is the real descriptor with "ILjava/lang/Object;" (mask+marker) inserted before the close. **Validated against the real APK**: the `StringsKt.replace$default` boundary is gone — the run progresses through the StringsKt core and stops at the next unimplemented API: `Ljava/lang/Class;->forName(Ljava/lang/String;)Ljava/lang/Class;` (dynamic class loading by name — explicitly excluded from the Class work unit; needs a name→descriptor index across loaded guest classes; the KnownStaticShape hygiene entry ensures it reports as a clean unimplemented boundary rather than a misleading shape mismatch).
42. **Class.forName**: revert the `forName` bindings in `JavaLangBindings.cs` (1-arg + 3-arg), the `ForName`/`DescriptorFromBinaryName` helpers, the interpreter hooks (`HasGuestClass`, `EnsureGuestClassInitialized`), the `ClassNotFoundException` Parents entry (extends `Ljava/lang/Exception;` directly — checked in real Java, same "get the superclass chain right, don't invent a checked/unchecked distinction" note as InterruptedException), the README portfolio/boundary changes, and the forName tests in `ClassTests.cs` together. Revert returns both forName overloads to unimplemented. Semantics: dotted binary name → internal descriptor (reverse of the Class.getName() formatting already built; bounded array-name handling converts dotted class components), lookup via the interpreter's own `_dexSet.FindClass` (HasGuestClass), found → `EnsureClassInitialized` as the REAL documented side effect (unless the 3-arg `initialize=false` skips it — the ClassLoader argument is accepted and unused, this runtime has no classloader hierarchy) → returns the canonical `EnsureClassObject` (never a fresh one, so reference identity with getClass()/const-class holds); not found, including framework/API-bound type names (no reverse name index exists — honest, correctly-scoped limitation) → checked `ClassNotFoundException`. Probe result: SKYNET references BOTH forName overloads (3-arg via bundled libs) — both bound; `Class.newInstance` is also referenced but remains out of scope (reflection, per the Class brief). **Validated against the real APK**: the `forName` boundary is gone — the run progresses and stops at the next unimplemented API: `Ljava/lang/Class;->getDeclaredMethods()[Ljava/lang/reflect/Method;` (real reflection — Method[] objects, a large separate feature, consistently out of scope).
43. **Bounded java.lang.reflect (lifecycle observer scan)**: revert `ApiLayer\Bindings\JavaLangReflectBindings.cs`, the `getDeclaredMethods`/`getInterfaces` Class bindings in `JavaLangBindings.cs`, the interpreter hooks (`DeclaredMethodsOf` — Direct+Virtual excluding `<init>`/`<clinit>` per the real JDK contract, empty for framework; `DeclaredInterfacesOf` — direct interfaces from `DexClass.Interfaces`), the `MethodPeer`/`Methods` quota (1024) in `AndroidFrameworkState`, the KnownStaticShape `Method` entry, the CreateBuilder registration, the README portfolio/boundary changes, and the ClassTests additions (incl. the lifecycle-scan reproduction) together. Revert returns all reflective calls to unimplemented. Scope was decided from the orchestrator's investigation (caller chain `ComponentActivity.<init>` → `LifecycleRegistry.addObserver` → `Lifecycling.lifecycleEventObserver` → `ReflectiveGenericLifecycleObserver.<init>` → `ClassesInfoCache.getInfo` → `createInfo` → `getDeclaredMethods`) and a full-APK annotation-usage scan proving NO class in SKYNET or its bundled libs declares `@OnLifecycleEvent` — so on the observed path `getAnnotation` always returns null, no callback registers, and `Method.invoke` is never reached. `Method.getAnnotation` is bound to ALWAYS null, documented precisely: this runtime does not model annotation metadata at all (DEX annotation parsing is not implemented anywhere), so "not present" is the honest consequence — NOT "annotations don't exist"; a future boundary needing real annotation presence/metadata is real DEX annotation parsing, a separate larger work unit. Fresh `Method` objects per call (real Java does not guarantee Method reference identity across separate reflective calls). Deliberately NOT bound (confirmed out of scope by the investigation): `Method.invoke` (provably unreachable on this path), `getModifiers`/`Modifier.*` (JSONObject.populateMap's path, not on the `<init>` boundary), `getClassLoader`/`getMethods`/`getDeclaredFields`, any real Annotation model, okio's Options reflection. **Validated against the real APK**: the `getDeclaredMethods` boundary is gone — the run progresses through the lifecycle scan (getDeclaredMethods + getInterfaces + getAnnotation) and stops at the next unimplemented API: `Ljava/lang/Boolean;->valueOf(Z)Ljava/lang/Boolean;` (Boolean boxing — a representation gap related to the Integer/Long boxing boundary flagged earlier, reached inside ClassesInfoCache.createInfo's cache; exactly the kind of further `<init>`-chain boundary the orchestrator flagged as expected).
44. **Primitive boxing (Boolean/Integer/Long/Short/Byte/Character/Double/Float)**: revert `ApiLayer\Bindings\JavaLangBoxingBindings.cs`, the `BoxedPeer`/`Boxed` quota (1024) + `BoxedObject` cache in `AndroidFrameworkState`, the static-field resolver additions (Boolean.TRUE/FALSE, per-type TYPE), the `NumberFormatException` Parents entry (extends `IllegalArgumentException`, checked in real Java), the KnownStaticShape boxed-class entries (static valueOf/parse*/primitive-param statics vs instance accessors), the CreateBuilder registration, the README portfolio/boundary changes, and `BoxingTests.cs` together. Revert returns all boxing to unimplemented. Root cause confirmed by the orchestrator: every Dalvik category-1 value is stored uniformly as a C# int, so a raw value cannot disambiguate Integer/Short/Byte/Character/Boolean — a tagged wrapper is required; the box is a DexObject typed as the box class with a `BoxedPeer` holding the raw value (same peer pattern as AtomicReference etc.; NO DynamicDescriptor change needed — the DexObject branch already covers it). Caching contract is the REAL documented JDK behavior, per type, NOT a uniform rule: Boolean always two singletons; Integer/Short/Byte/Long.valueOf cache -128..127; Character.valueOf caches 0..127 (unsigned asymmetry, verified); Double/Float.valueOf never cache. Equality is VALUE-based (Integer.equals compares the value — unlike Class/Enum which are identity); hash codes are the documented formulas (Boolean 1231/1237, Long `(int)(v^(v>>32))`, Float `floatToIntBits`, Double `(int)(bits^(bits>>32))` with doubleToLongBits). Parse methods throw guest `NumberFormatException` on failure (parseBoolean returns false for anything not "true" case-insensitive — real contract, no throw). Probe result: all eight types are referenced across SKYNET's libs; the core Number surface was built per type (valueOf incl. valueOf(String), same-type unboxing, equals, hashCode instance + primitive-param static, toString instance + static incl. radix 2/8/16, parse, compareTo/compare with real NaN ordering and -0.0<0.0, Double/Float isNaN/isInfinite, Boolean.compare/toString(Z)/hashCode(Z)/parseBoolean). Bounded choices: parseInt/parseLong with non-2/8/10/16 radix fall back to decimal (documented — .NET Convert only supports 2/8/10/16; a full radix engine not built); Double/Float toString uses "R" round-trip (close to Java, exponent notation can differ — consistent with String.valueOf(D/F)); Integer/Long bit operations, Character Unicode static predicates, and Double/Float bit methods are referenced by libs but deliberately NOT built (separate features — report if the run reaches them). **Validated against the real APK**: the `Boolean.valueOf` boundary is gone — the run progresses through the boxing surface (reached inside ClassesInfoCache.createInfo's cache) and stops at the next unimplemented API: `Ljava/util/Map;->entrySet()Ljava/util/Set;` (a java.util.Map method — next work unit).
45. **java.util.Map views + Map.Entry**: revert `ApiLayer\Bindings\JavaUtilMapBindings.cs`, the `HashMapPeer.Entries()` enumeration accessor (sentinel-un-translating), the `MapEntryPeer`/`MapEntries` (1024) + `MapViews` (256) stores in `AndroidFrameworkState`, the CreateBuilder registration, the README portfolio/boundary changes, and `MapViewTests.cs` together. Revert returns all Map views and Map.Entry to unimplemented. Design: entrySet/keySet/values are SNAPSHOT views (capture at call time, the same convention Iterator established for ArrayList) materialized as Set/Collection-shaped peers (`MapViews` store of `HashSet<object?>`) supporting iterator()/size()/contains()/isEmpty() — for-each iteration (the overwhelmingly common real usage) works; the views are deliberately NOT live write-through views (mutating the view does not mutate the backing map — real Java behavior but a real complexity jump, snapshot-only is the honest bounded scope, confirmed sufficient for the probe's observed usage). Map.Entry is getKey/getValue on a `MapEntryPeer` snapshot; setValue NOT bound (probe shows no Map$Entry method references; write-through not needed). Both bound map classes feed ONE view path: `HashMapPeer` gained an enumeration accessor translating its private null-key sentinel back to null (mirroring Get/Remove), WeakHashMapPeer's public Entries is consumed directly — no per-map view duplication. Probe-confirmed Map commons bound for HashMap/WeakHashMap/Map ids: getOrDefault, remove(Object,Object) (removes only when the value matches — real semantics), containsValue, isEmpty, clear, putAll, HashMap.equals/hashCode (value-based map equality: same size + every key/value pair matches; hash = sum of key^value hashes). The basic accessors (put/get/containsKey/remove/size) exist per-concrete-class in the monolith; mirrored on the Map interface ids so Map-typed call sites resolve. `Iterator.remove` (referenced by libs) is NOT bound — a snapshot iterator cannot remove from the backing map (would need write-through). Bounded note: WeakHashMap's existing bindings do not support null keys (real WeakHashMap throws NPE; the pre-existing monolith bindings crash with a CLR ArgumentNullException — unchanged, documented). **Validated against the real APK**: the `Map.entrySet` boundary is gone — the run progresses and stops at the next unimplemented API: `Lkotlin/LazyKt;->lazy(Lkotlin/jvm/functions/Function0;)Lkotlin/Lazy;` (kotlin.Lazy — the lazy delegate; kotlin stdlib, likely R8-shrunk like StringsKt; next work unit).
46. **kotlin.Lazy**: revert `ApiLayer\Bindings\KotlinLazyBindings.cs`, the `LazyPeer`/`Lazies` quota (256) in `AndroidFrameworkState`, the CreateBuilder registration, the README portfolio/boundary changes, and `LazyTests.cs` together. Revert returns all Lazy calls to unimplemented. Probe-confirmed shape: only `LazyKt.lazy(Function0)Lazy`, `Lazy.getValue()Object`, and `Lazy.isInitialized()Z` are referenced — NO explicit `LazyThreadSafetyMode` overload and NO `getValue(Lazy,Object,KProperty)` delegate-operator extension (the real call sites call `Lazy.getValue()` directly), so only those three are built. getValue() reuses the established guest-functional-interface invocation pattern (`state.Interpreter.InvokeInstanceExact(func0, "invoke", "()Ljava/lang/Object;")` — the same one-line shape Thread uses for Runnable.run(), per the orchestrator's own check of JavaLangThreadBindings); computes on first access, caches afterward. No real thread-safety mechanism is built, documented honestly: the GIL serializes all guest bytecode execution, so a plain "if not computed, compute and cache" is correct under all three real LazyThreadSafetyMode values (SYNCHRONIZED/PUBLICATION/NONE) — no observable difference exists under this execution model, same reasoning as monitor-enter/AtomicReference. **Validated against the real APK**: the `LazyKt.lazy` boundary is gone — the run progresses and stops at the next unimplemented API: `Ljava/util/Collections;->synchronizedMap(Ljava/util/Map;)Ljava/util/Map;` (java.util.Collections — next work unit).

47. **java.util.Collections**: revert `ApiLayer\Bindings\JavaUtilCollectionsBindings.cs`, the `ListPeer`/`HashMapPeer` Unmodifiable flags + `RequireMutable` + shared-backing constructors in `AndroidFrameworkState`, the `UnsupportedOperationException` Parents entry (extends RuntimeException), the monolith ArrayList/HashMap write bindings' RequireMutable checks, the KnownStaticShape `Collections` static entry, the CreateBuilder registration, the README portfolio/boundary changes, and `CollectionsTests.cs` together. Revert returns all Collections calls to unimplemented. TWO families with genuinely different contracts, deliberately not conflated: (1) `synchronizedMap` returns the SAME underlying map object � under this runtime's GIL all guest bytecode already executes serialized, so no synchronization wrapper is needed (real architectural consequence of the GIL, same reasoning as Lazy/monitor-enter; only synchronizedMap is referenced by the probe � no synchronizedList/Set/Collection); (2) `unmodifiableList`/`unmodifiableMap`/`unmodifiableSet` + `emptyList`/`emptyMap`/`emptySet` + `singletonList`/`singletonMap`/`singleton` enforce REAL immutability � wrappers sharing the backing peer (ListPeer/HashMapPeer shared-backing constructors, so reads stay live) whose writes throw guest `UnsupportedOperationException` (add/set/remove/clear on List, put/remove/clear/putAll/remove(k,v) on Map via RequireMutable checks added to the monolith + JavaUtilMapBindings helpers); every Set this runtime produces (snapshot views, unmodifiable, singleton, empty) is immutable, so Set.add/remove/clear always throw. Also probe-confirmed: sort(List) (natural ordering: strings ordinal, boxed numerics by value, DexObject tries its guest compareTo � bounded; non-Comparable compares equal rather than ClassCastException) / sort(List,Comparator) (Comparator.compare invoked via the guest-functional-interface pattern), reverse, max/min (+Comparator, guest NoSuchElementException on empty), fill, addAll(Collection,Object[]) (ArrayList targets add; other targets throw UOE), newSetFromMap (bounded: independent HashSet � the JSONObject recursion-avoidance add/contains use works; NO write-through to the backing map and NO identity semantics, documented). Probe result: binarySearch/shuffle (depend on Comparable/Random unmodeled machinery), list(Enumeration) (Enumeration unmodeled), and EMPTY_LIST/EMPTY_MAP/EMPTY_SET static fields (not referenced) are deliberately NOT built. **Validated against the real APK**: the `Collections.synchronizedMap` boundary is gone � the run progresses and stops at the next unimplemented API: `Ljava/util/ArrayDeque;-><init>()V` (java.util.ArrayDeque � next work unit).
No commits are required for these boundaries; each is a coherent reversible behavior unit.
# Production boundaries added in the current work units

## Android retained UI foundation

The portable Core now includes the first retained Android scene boundary under `AndroidRuntime.Core.Ui`: stable `AndroidViewNode` identities, `AndroidMeasureSpec`, deterministic dp/sp metrics, horizontal/vertical `AndroidLinearLayoutNode`, `AndroidTextViewNode`, `AndroidButtonNode`, immutable typed display lists, recording renderer, coalesced invalidation, retained frame revisions with stale-frame rejection, semantic snapshots, hit testing, and hostile-tree/display/invalidation quotas. The architecture follows the retained-host and renderer-port roles studied in AetherUI without referencing that repository or importing HTML/DOM/CSS semantics. No AetherUI source was copied.

`ApkLoader` also exposes bounded immutable snapshots for optional `resources.arsc` and canonical `res/` files, with entry/per-file/aggregate/compression-ratio quotas. `AndroidBinaryXmlReader` provides a generic event stream and retained tree for compiled XML. `AndroidResourceTable` validates package/type/typeSpec chunks and resolves simple string, color, dimension, integer, boolean, and reference values; `AndroidResourceResolver` selects default/density configurations deterministically, detects reference cycles, enforces depth/count/string-byte quotas, and loads canonical `res/layout*.xml` entries by symbolic name or resource ID. `AndroidLayoutInflater` maps the supported compiled XML elements and attributes into scene nodes without fixture constants. Per-session bidirectional DEX View peers remain stable until teardown, declarative `android:onClick` resolves only an exact public Activity callback, and all guest UI callbacks execute on the Android execution lane. Text mutation invalidates layout/display data without reinflating the tree.

The Windows host mounts a dedicated child HWND for Android pixels; no renderer targets the WPF top-level window. The Android lane publishes immutable revisioned display snapshots through a single-post coalescer, stale revisions are rejected, and pointer/keyboard work is enqueued back to the lane before a resulting snapshot is requested. Rendering and capture share the same deterministic BGRA backing representation at 96/120/144 DPI. The current backend is the bounded software fallback, not AetherUI's Direct2D/WARP implementation: solid rectangles and deterministic text-cell painting are supported, while DirectWrite shaping, GPU device recovery, individual UIA fragment providers, complex/bag entries, sparse type chunks, locale/night/orientation selection, overlays, themes/styles, drawable resources, and scrolling remain explicit subsequent work. No AetherUI source is copied or linked.

The signed `UiProbe.apk` fixture is built from real `aapt2`-compiled `LinearLayout`/`TextView`/`Button` XML plus string, color, dimension, ID, and layout resources. Its build runs `aapt2 dump xmltree`, `aapt2 dump resources`, and `apksigner verify`; tests discover resource IDs and compiled file paths from the table rather than embedding fixture IDs.

## Reverse Activity lifecycle

Hosted sessions execute `onPause()V`, `onStop()V`, and `onDestroy()V` on the per-session Android execution lane before cancellation and peer disposal. The explicit state machine is `Constructed -> Created -> Started -> Resumed -> Paused -> Stopped -> Destroyed`, with `Faulted` terminal failure. Termination is idempotent and runs only the valid suffix from the last successful forward checkpoint. `Activity.finish()V`, `isFinishing()Z`, and `isDestroyed()Z` are exact bounded bindings. A WPF user-close request is deferred until guest teardown completes. This is not a full Android task/back-stack or configuration-change implementation.

## Governed power services

Power access is denied by default and enabled explicitly by the host CLI with `--grant-power`. `Context.getSystemService("batterymanager")` and `Context.getSystemService("power")` return stable, session-owned peers only when the host adapter is available and policy permits lookup. Exact bindings are:

- `BatteryManager.getIntProperty(I)I`
- `BatteryManager.getLongProperty(I)J`
- `BatteryManager.isCharging()Z`
- `PowerManager.isPowerSaveMode()Z`

The Core port is an immutable `HostPowerSnapshot`. Windows uses `GetSystemPowerStatus`; capacity, charging, battery presence, and saver mode are reported only when the OS exposes them. Unsupported int properties return `0` for target SDK below 28 and `Integer.MIN_VALUE` otherwise; unsupported long properties return `Long.MIN_VALUE`. Audit events contain operation/result metadata only. Interactive/idle state and wake locks are intentionally deferred.

## Structural DEX verification

Every production launch now passes through `DexVerifier` before parsing/execution. It validates the standard SHA-1 signature and Adler-32 checksum, supported DEX version and complete header, every map item identity/order/range, declared sections, parse integrity, and every method instruction stream. All interpreter opcodes share explicit format/width metadata; opcodes without metadata fail closed as unsupported. Format-specific checks cover every register operand and wide pair, pool references, branch alignment, immediate typed `move-result`, invoke word/range/`outs_size` consistency, payload bounds, and strict try/handler checks. Verification has deterministic file/method/instruction budgets and stable malformed-versus-unsupported diagnostics. `scripts/fuzz-dex.ps1` runs the bounded seeded mutation corpus.

This is deliberately **not** the full ART type verifier: it does not prove all data-flow types, monitor discipline, or every verifier rule for opcodes outside the interpreter portfolio. Guest class initialization itself now runs in the interpreter (`<clinit>()V` executes exactly once per session at the new-instance/static-field/static-method triggers, superclass before subclass, single-lane cycle handling), but the verifier does not prove its correctness, and reflection-based triggers (`Class.forName` with initialize=true) are not modeled.

## Windows packaging

`scripts/package-win-x64.ps1` creates a self-contained, multi-file `win-x64` Release package using `Properties/PublishProfiles/win-x64.pubxml`. Trimming, single-file bundling, ReadyToRun, and product PDBs are explicitly disabled. Output is restricted to a non-reparse descendant of the repository-local `artifacts` directory before any recursive cleanup. The staged package includes sorted shipped-file and dependency inventories, an explicit signing-status marker, and a deterministic ZIP with fixed entry timestamps/order. `SHIPPED-FILES.txt` declares its self-reference semantics; the final archive SHA-256 is emitted beside, not recursively inside, the ZIP. Signing is optional through `-SigningHook`; without it the package is explicitly unsigned. The dependency inventory is not claimed to be a standards-format SBOM.

Rollback boundaries are independent: reverse lifecycle/session shutdown, power ports/bindings/Windows adapter, verifier/runtime gate/fuzz corpus, and publish profile/package script can each be reverted without reverting the other work units.
48. **java.util.ArrayDeque**: revert `ApiLayer\Bindings\JavaUtilArrayDequeBindings.cs`, the `ArrayDeques` quota (256) in `AndroidFrameworkState`, the `AndroidPeerStore.TryGet` addition, the shared `RequireSetBacking` resolver + unmodifiableSet multi-store support in `JavaUtilMapBindings`/`JavaUtilCollectionsBindings`, the KnownStaticShape ArrayDeque/Deque/Queue entries, the CreateBuilder registration, the README portfolio/boundary changes, and `ArrayDequeTests.cs` together. Revert returns all ArrayDeque calls to unimplemented. Design: reuses the shared `ListPeer` shape (double-ended ops map directly onto `List<object?>` � addFirst=insert 0, addLast=append, removeFirst/removeLast at the ends). Two real contracts honored distinctly, NOT conflated: (1) `getLast`/`remove`/`removeFirst`/`removeLast`/`pop` THROW guest `NoSuchElementException` on empty (real Java; pop is documented as removeFirst) while `peek`/`poll`/`pollFirst` return null on empty; (2) ArrayDeque does NOT permit null elements (unlike ArrayList � a real, spec-documented difference) � every add-style method (add/addFirst/addLast/offer/push/<init>(Collection)) throws guest `NullPointerException` for null. Probe result: the APK uses a MIXED style (add/addFirst/addLast + peek/poll/pollFirst/pop/push/remove/removeFirst/removeLast/getLast � no offerFirst/offerLast/peekFirst/peekLast/getFirst/element); built the referenced surface plus the Queue/Deque interface ids (add/offer/poll/peek/pop/push/removeFirst/isEmpty/iterator, remove(Object)) so interface-typed call sites resolve. NOT built (not referenced): getFirst, peekFirst/peekLast, offerFirst/offerLast, element, removeFirstOccurrence/removeLastOccurrence, descendingIterator, array conversion. Validation also found and fixed a REAL bug the Map-views work had introduced: the shared `Set.iterator`/`size`/`contains`/`isEmpty` bindings read ONLY the MapViews store, breaking the existing CopyOnWriteArraySet (whose peers live in CopyOnWriteArraySets) when a Set-typed call site resolved to them � fixed with `AndroidPeerStore.TryGet` + a `RequireSetBacking` resolver covering BOTH Set-shaped stores, and unmodifiableSet similarly accepts either source. **Validated against the real APK**: the `ArrayDeque` boundary is gone � the run progresses and stops at the next unimplemented API: `Ljava/util/LinkedHashSet;-><init>()V` (java.util.LinkedHashSet � next work unit).
49. **java.util.LinkedHashSet**: revert `ApiLayer\Bindings\JavaUtilLinkedHashSetBindings.cs`, the `OrderedSetPeer`/`LinkedHashSets` quota (256) in `AndroidFrameworkState`, the KnownStaticShape LinkedHashSet entry, the CreateBuilder registration, the README portfolio/boundary changes, and `LinkedHashSetTests.cs` together. Revert returns all LinkedHashSet calls to unimplemented. DESIGN POINT per the brief: iteration order is the entire reason LinkedHashSet exists � the raw `HashSet<object?>` peer used by CopyOnWriteArraySet was deliberately NOT reused because .NET does not reliably guarantee HashSet enumeration order; instead a dedicated `OrderedSetPeer` (List<object?> keeping order + linear Contains check before Add for no-duplicate semantics) is simple, obviously correct, and fast enough at this runtime's bounded quotas. Real semantics honored and tested: a duplicate add returns false and does NOT move the element (insertion order of first occurrence, NOT most-recent-touch � different from LinkedHashMap's optional access-order mode which does not apply); remove then re-add DOES move the element to the end (fresh insertion). Probe result: referenced surface is <init>()V/<init>(I)V/<init>(Collection), add, addAll, clear, isEmpty, iterator, remove, removeAll � size/contains are NOT referenced and were omitted by discipline (build if the run reaches them). **Validated against the real APK**: the `LinkedHashSet` boundary is gone � the run progresses and stops at the next unimplemented API: `Ljava/util/LinkedHashMap;-><init>(IFZ)V` (java.util.LinkedHashMap � insertion-ordered map, next work unit).
50. **java.util.LinkedHashMap**: revert `ApiLayer\Bindings\JavaUtilLinkedHashMapBindings.cs`, the `LinkedHashMapPeer`/`LinkedHashMaps` quota (256) in `AndroidFrameworkState`, the JavaUtilMapBindings shared-helper wiring for LinkedHashMap (MapPairs/MapCount/ClearMap/PutValue/GetValue/ContainsKey/RemoveValue/PutAll/RemoveIfValue/GetOrDefault dispatchers + RegisterFor/RegisterMapCommons calls + MapPairsFor + MapEquals), the KnownStaticShape LinkedHashMap entry, the CreateBuilder registration, the README portfolio/boundary changes, and `LinkedHashMapTests.cs` together. Revert returns all LinkedHashMap calls to unimplemented. INVESTIGATION RESULT (explicitly reported): the failing 3-arg constructor (int initialCapacity, float loadFactor, boolean accessOrder) is called by `androidx.collection.LruCache.<init>` with accessOrder=true; the full-APK scan found NO guest subclass of LinkedHashMap and NO removeEldestEntry override/reference anywhere (LruCache in this build manages eviction MANUALLY via trimToSize, not via the protected removeEldestEntry hook) � so the scope built is the PLAIN ACCESS-ORDER case, with NO LRU-eviction callback machinery (proven unused, same discipline as every reflection-adjacent boundary). Semantics: LinkedHashMapPeer mirrors HashMapPeer (private dictionary + null-key sentinel, CLR equality for keys) + a parallel key-order List (mirrors OrderedSetPeer's reasoning); accessOrder=false (default) = insertion order with get/put on an existing key NOT reordering; accessOrder=true = ACCESS order � every successful get AND put (including updating an existing key) moves the entry to most-recently-used-last. Views (entrySet/keySet/values) iterate the map's ACTUAL order via the peer's ordered Entries() (never an unordered scan); the shared Map commons + basic accessors are wired for LinkedHashMap in JavaUtilMapBindings's helpers (MapPairsFor exposed for the <init>(Map)/putAll paths). The float loadFactor/int initialCapacity ctor args are accepted and unused (bounded � this runtime does not size or rehash maps). Probe result: <init>()V/<init>(I)/<init>(IFZ)/<init>(Map), containsKey, entrySet, get, isEmpty, keySet, put, putAll, remove, size, values � all bound. **Validated against the real APK**: the `LinkedHashMap.<init>(IFZ)` boundary is gone � the run progresses and stops at the next unimplemented API: `Ljava/util/WeakHashMap;-><init>(I)V` (the 1-arg WeakHashMap constructor, not yet bound � next work unit). **Follow-up (one-line binding)**: `WeakHashMap.<init>(I)V` is now bound in the monolith next to `<init>()V` (the int initialCapacity is accepted and ignored � this runtime does not size or rehash maps, same pattern as `ArrayList.<init>(I)`); the run then progresses to the next boundary: `Landroid/view/LayoutInflater;->from(Landroid/content/Context;)Landroid/view/LayoutInflater;` (android.view UI inflation � next work unit).
